package Algorithm.AlgorithmState;

public class State {
    /*
    //给出求整形数组a中最大值的算法
    public static int maxEle(int[] a){
        int n = a.length;
        int max = a[0];
        for (int i = 0;i < n;i ++){
            if (max < a[i])
                max = a[i];
        }
        return max;
    }

    //给出将整型数组a中数据元素实现就地逆置的算法
    public static void reverse(int[] a){
        int n = a.length;
        int temp;
        for (int i = 0;int j = n - 1;i < j;int i ++,j --){
            temp = a[i];
            a[i] = a[j];
            a[j] = temp;
        }
    }

     */
}
